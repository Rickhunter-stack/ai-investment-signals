# Ingestion trust boundary

External article text is evidence, not executable instruction. Structured events must conform to the repository schema and validator. URLs are stored as provenance only. The ingestion helper does not fetch or execute source content.
