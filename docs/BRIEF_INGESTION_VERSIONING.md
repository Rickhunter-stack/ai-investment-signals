# Versioning

`brief-event-v1` remains the event schema version. `brief-ingestion-v1` names the transport/append contract. Future schema changes must use a new schema version rather than silently changing the meaning of frozen historical fields.
