# Acceptance criteria

- A valid brief-event-v1 object can be appended to the correct monthly journal.
- Existing frozen events remain byte-for-byte equivalent as parsed JSON.
- Duplicate IDs and invalid events are rejected before write.
- No existing weekly score, signal research, market/fundamental collector or dashboard behavior is changed.
- Daily automation activation occurs only after merge.
