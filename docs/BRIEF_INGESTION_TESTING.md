# Testing

Run `python -m unittest discover -s scripts -p 'test_brief*.py'` and `python scripts/validate_brief_events.py`. The tests cover valid append, duplicate rejection, invalid-source rejection and historical edit rejection.
