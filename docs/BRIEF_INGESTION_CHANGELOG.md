# Brief ingestion changelog

## brief-ingestion-v1

Adds a safe append helper, ingestion tests and the structured-output contract needed to connect the existing ChatGPT daily brief to `data/brief_events/YYYY-MM.json`. No historical events are backfilled and no scoring/dashboard behavior changes.
